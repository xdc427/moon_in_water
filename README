在user_code目录下为每一个影子模块新建一个目录，在编译时会在user_code下
生成同名的影子模块名相关的目录，最后会在user_code下生成所有模块目标文件。

定义了一种同步数据类型，用宏定义的几种方式操作不需要考虑底层数据传输能实现多个影子镜像的同步。
1。先把SHADOW_STRUCT定义的同步类型和c的基本类型分析加入类型集合。
2。分析所有的SHADOW_VAR和SHADOW_NEW的数据拷贝到一块内存，并与上个版本xor然后用gz压缩。
